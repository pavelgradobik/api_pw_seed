export class baseController {


    request() {
        // const prepare fullurl

        //prepare request =new RequestBuilder().prepareFullUrl(someUrl)

        // prepare token and add it to the request request.headers({token})
    }
}